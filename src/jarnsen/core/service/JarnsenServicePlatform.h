#pragma once

// Transitional compatibility include. Platform selection now belongs to the
// hardware layer; existing Core/service callers can migrate without a flag day.
#include "jarnsen/hardware/JarnsenServicePlatform.h"

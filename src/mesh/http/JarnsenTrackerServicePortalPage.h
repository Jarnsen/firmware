#pragma once

#include <Arduino.h>

static const char JARNSEN_TRACKER_PORTAL_PAGE[] PROGMEM = R"HTML(<!doctype html>
<html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#0b1220">
<title>Jarnsen Tracker Service</title><style>
:root{color-scheme:light dark;--bg:#eef2f7;--card:#fff;--fg:#17212b;--muted:#677586;--line:#d8e0e8;--accent:#1463d6;--ok:#16834a;--warn:#c26a00;--bad:#c93434;--chip:#eaf1f8}
@media(prefers-color-scheme:dark){:root{--bg:#0b1220;--card:#121d2f;--fg:#eaf2ff;--muted:#91a6c2;--line:#263a55;--accent:#38bdf8;--ok:#3ddc97;--warn:#ffb454;--bad:#ff6b77;--chip:#182741}}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--fg);font:15px system-ui,-apple-system,"Segoe UI",sans-serif}.wrap{max-width:860px;margin:auto;padding:12px 12px 38px}.bar{position:sticky;top:0;z-index:20;margin:-12px -12px 12px;padding:10px 12px;background:color-mix(in srgb,var(--card) 94%,transparent);border-bottom:1px solid var(--line);backdrop-filter:blur(12px);display:flex;align-items:center;gap:8px}.grow{min-width:0;flex:1}.bar b,.bar small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.bar small,.muted,.status{color:var(--muted)}h1{font-size:24px;margin:3px 0}.nav,.actions,.mapmodes,.quick{display:flex;gap:7px;flex-wrap:wrap}.nav{overflow:auto;padding:3px 0 6px;flex-wrap:nowrap}.nav a,.chip{white-space:nowrap;background:var(--chip);color:var(--fg);padding:8px 10px;border-radius:999px;text-decoration:none;border:1px solid var(--line)}.card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:14px;margin:12px 0;box-shadow:0 2px 12px #0001}.card h2{font-size:18px;margin:0 0 10px}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.metric{border:1px solid var(--line);border-radius:12px;padding:10px;min-width:0;color:var(--muted)}.metric b{display:block;color:var(--fg);font-size:18px;margin-top:3px;overflow:hidden;text-overflow:ellipsis}button,.button,input{font:inherit}button,.button{appearance:none;border:0;border-radius:10px;background:var(--accent);color:#fff;padding:10px 12px;font-weight:650;text-decoration:none;text-align:center;cursor:pointer}button.secondary,.button.secondary{background:transparent;color:var(--accent);border:1px solid var(--accent)}button.danger{background:var(--bad)}button.active{outline:2px solid var(--fg);outline-offset:1px}button:disabled{opacity:.45}.status{min-height:21px;margin-top:8px}.ok{color:var(--ok)}.warn{color:var(--warn)}.err{color:var(--bad)}.filters{display:grid;grid-template-columns:1.4fr 1fr 1fr;gap:7px;margin:9px 0}.filters label{font-size:12px;color:var(--muted)}.filters input{display:block;width:100%;margin-top:3px;padding:9px;border:1px solid var(--line);border-radius:9px;background:var(--bg);color:var(--fg)}.mapbox{position:relative;height:390px;border:1px solid var(--line);border-radius:13px;overflow:hidden;background:#d8dde3;margin-top:8px}.mapbox canvas{width:100%;height:100%;display:block;touch-action:none}.mapbadge{position:absolute;z-index:4;left:8px;top:8px;background:color-mix(in srgb,var(--card) 90%,transparent);border:1px solid var(--line);border-radius:8px;padding:5px 7px;font-size:12px;color:var(--muted);pointer-events:none}.attrib{font-size:11px;color:var(--muted);min-height:17px;margin-top:4px}.pointinfo{min-height:42px;padding:8px 1px;color:var(--muted)}progress{width:100%;height:12px;margin-top:10px}pre{white-space:pre-wrap;word-break:break-word;max-height:38vh;overflow:auto;background:var(--bg);border-radius:10px;padding:10px;font:12px ui-monospace,monospace}.hide{display:none}.dangerzone{border-color:color-mix(in srgb,var(--bad) 35%,var(--line))}@media(max-width:620px){.grid{grid-template-columns:repeat(2,minmax(0,1fr))}.filters{grid-template-columns:1fr 1fr}.filters label:first-child{grid-column:1/-1}.mapbox{height:330px}.actions>*{flex:1 1 145px}}@media(max-width:390px){.grid{grid-template-columns:1fr 1fr}.metric b{font-size:16px}}
</style></head><body><main class="wrap">
<div class="bar"><div class="grow"><b id="barName">Jarnsen Tracker</b><small id="barState">WLAN SERVICE · 192.168.4.1</small></div><button class="danger" onclick="shutdownWifi()">WLAN beenden</button></div>
<h1>Jarnsen Mobile Service</h1><div class="muted" id="device">Gerät wird gelesen …</div><nav class="nav"><a href="#overview">Übersicht</a><a href="#map">Karte</a><a href="#diag">Diagnose</a><a href="#mesh">Mesh Health</a><a href="#firmware">Firmware</a></nav>
<section class="card" id="overview"><h2>Übersicht</h2><div class="grid"><div class="metric">Node<b id="mNode">–</b></div><div class="metric">Rolle<b id="mRole">–</b></div><div class="metric">WLAN<b id="mSsid">–</b></div><div class="metric">Positionen<b id="mPoints">0</b></div><div class="metric">Boots<b id="mBoots">–</b></div><div class="metric">Crash-Resets<b id="mCrash">–</b></div></div><div id="overviewStatus" class="status">Lokale Servicedaten bereit.</div></section>
<section class="card" id="map"><h2>Positionskarte</h2><div class="mapmodes"><button data-map="osm" onclick="setMapMode('osm')">OSM</button><button data-map="topo" onclick="setMapMode('topo')">Topo</button><button data-map="sat" onclick="setMapMode('sat')">Satellit</button><button data-map="hybrid" onclick="setMapMode('hybrid')">Hybrid</button><button data-map="offline" onclick="setMapMode('offline')">Offline</button></div>
<div class="filters"><label>Datum<input id="trackDate" type="date" onchange="applyFilter()"></label><label>Von<input id="trackFrom" type="time" value="00:00" onchange="applyFilter()"></label><label>Bis<input id="trackTo" type="time" value="23:59" onchange="applyFilter()"></label></div><div class="quick"><button class="secondary" onclick="quickFilter(3600)">1 h</button><button class="secondary" onclick="quickFilter(21600)">6 h</button><button class="secondary" onclick="quickFilter(86400)">24 h</button><button class="secondary" onclick="quickFilter(604800)">7 Tage</button><button class="secondary" onclick="resetFilter()">Alle</button></div>
<div class="mapbox"><canvas id="trackMap"></canvas><div class="mapbadge" id="mapBadge">Offline</div></div><div class="attrib" id="attrib"></div><div class="pointinfo" id="pointInfo">Punkte werden geladen …</div><div class="actions"><button onclick="loadTrack()">Neu laden</button><button class="secondary" onclick="fitTrack()">Auf Auswahl zoomen</button><button class="secondary" onclick="zoomMap(1)">+</button><button class="secondary" onclick="zoomMap(-1)">−</button><a class="button secondary" href="/track.geojson" download="Jarnsen-Positionsverlauf.geojson">GeoJSON</a><button class="danger" onclick="clearTrack()">Verlauf löschen</button></div><div id="trackStatus" class="status"></div></section>
<section class="card" id="diag"><h2>Diagnose</h2><div class="actions"><button onclick="analyse()">Log laden &amp; auswerten</button><a class="button secondary" href="/log" download>Log speichern</a><button class="danger" onclick="clearLog()">Log löschen</button></div><div id="logStatus" class="status"></div><div id="diagMetrics" class="grid hide"></div><pre id="raw" class="hide"></pre></section>
<section class="card" id="mesh"><h2>Mesh Health</h2><div class="grid"><div class="metric">Beobachtete Nodes<b id="hKnown">0</b></div><div class="metric">Aktiv 15 min<b id="h15">0</b></div><div class="metric">Aktiv 1 h<b id="h1h">0</b></div><div class="metric">Aktiv 24 h<b id="h24">0</b></div><div class="metric">Direkt 15 min<b id="hDirect">0</b></div><div class="metric">RX letzte Stunde<b id="hRx">0</b></div><div class="metric">Letzter direkt<b id="hLast">–</b></div><div class="metric">RSSI<b id="hRssi">–</b></div><div class="metric">SNR<b id="hSnr">–</b></div></div><div class="status">Passiver Empfangsmonitor; Routing und Antennentest werden nicht verändert.</div></section>
<section class="card"><h2>Langzeit-Diagnose</h2><div class="grid"><div class="metric">Service geöffnet<b id="dSvc">0</b></div><div class="metric">BLE-Verbindungen<b id="dBle">0</b></div><div class="metric">WLAN-Starts<b id="dWlan">0</b></div><div class="metric">WLAN-Fehler<b id="dWlanFail">0</b></div><div class="metric">Resetgrund<b id="dReset">–</b></div></div></section>
<section class="card" id="firmware"><h2>Firmwareupdate über WLAN</h2><p class="muted">Die passende aktuelle Firmware wird aus dem Jarnsen-GitHub-Release gewählt und vor Installation per SHA-256 geprüft.</p><div class="actions"><button id="githubBtn" onclick="githubUpdate()">Aktuelle GitHub-Firmware laden</button></div><div id="fallback" class="hide"><p class="warn">Der Captive-Browser konnte die GitHub-Datei nicht direkt weiterreichen. Datei laden und anschließend hier auswählen.</p><a id="downloadLink" class="button secondary" target="_blank">Firmware aus GitHub laden</a><input id="file" type="file" accept=".bin,application/octet-stream"><button onclick="uploadSelected()">Ausgewählte Firmware installieren</button></div><progress id="progress" class="hide" max="100" value="0"></progress><div id="fwStatus" class="status"></div></section>
<section class="card dangerzone"><h2>Service beenden</h2><p class="muted">Beendet den WLAN-AP kontrolliert. Der Tracker kehrt danach in seinen unveränderten Rollen- und Stromsparbetrieb zurück.</p><button class="danger" onclick="shutdownWifi()">WLAN jetzt beenden</button><div id="shutdownStatus" class="status"></div></section>
</main><script>
const API='https://api.github.com/repos/Jarnsen/firmware/releases/tags/';const $=id=>document.getElementById(id);let info=null,asset=null,allTrack=[],track=[],view={lat:49,lon:8,z:8},mapMode=localStorage.getItem('jtrk-map')||'offline',tileCache=new Map(),renderSerial=0;
function status(id,text,kind=''){const e=$(id);if(!e)return;e.textContent=text;e.className='status '+kind}function esc(s){return String(s??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}
async function boot(){try{const r=await fetch('/status',{cache:'no-store'});if(!r.ok)throw Error('HTTP '+r.status);info=await r.json();$('device').textContent=(info.long_name||info.title)+' · '+info.device+' · '+info.node_id;$('barName').textContent=info.long_name||'Jarnsen Tracker';$('mNode').textContent=info.short_name||info.node_id;$('mRole').textContent=info.role||'–';$('mSsid').textContent=info.ssid;$('mPoints').textContent=info.track_count;$('mBoots').textContent=info.health.boots;$('mCrash').textContent=info.health.crashes;$('hKnown').textContent=info.mesh.observed;$('h15').textContent=info.mesh.active15;$('h1h').textContent=info.mesh.active1h;$('h24').textContent=info.mesh.active24;$('hDirect').textContent=info.mesh.direct15;$('hRx').textContent=info.mesh.rx1h;$('hLast').textContent=info.mesh.last_node||'–';$('hRssi').textContent=info.mesh.last_age<4294967295?info.mesh.last_rssi+' dBm':'–';$('hSnr').textContent=info.mesh.last_age<4294967295?(info.mesh.last_snr_q4/4).toFixed(1)+' dB':'–';$('dSvc').textContent=info.health.service;$('dBle').textContent=info.health.ble;$('dWlan').textContent=info.health.wlan;$('dWlanFail').textContent=info.health.wlan_fail;$('dReset').textContent=info.health.reset;}catch(e){status('overviewStatus','Status nicht lesbar: '+e.message,'err')}setMapMode(mapMode);await loadTrack()}
function canvas(){const c=$('trackMap'),r=c.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2),w=Math.max(1,r.width),h=Math.max(1,r.height);if(c.width!==Math.round(w*d)||c.height!==Math.round(h*d)){c.width=Math.round(w*d);c.height=Math.round(h*d)}const x=c.getContext('2d');x.setTransform(d,0,0,d,0,0);return{c,x,w,h}}
function clampLat(v){return Math.max(-85.05112878,Math.min(85.05112878,v))}function world(lon,lat,z){const s=256*Math.pow(2,z),la=clampLat(lat)*Math.PI/180;return{x:(lon+180)/360*s,y:(1-Math.log(Math.tan(la)+1/Math.cos(la))/Math.PI)/2*s}}function unworld(x,y,z){const s=256*Math.pow(2,z),lon=x/s*360-180,n=Math.PI-2*Math.PI*y/s;return{lon,lat:180/Math.PI*Math.atan(Math.sinh(n))}}function toScreen(lat,lon,w,h){const p=world(lon,lat,view.z),c=world(view.lon,view.lat,view.z),s=256*Math.pow(2,view.z);let dx=p.x-c.x;if(dx>s/2)dx-=s;if(dx<-s/2)dx+=s;return{x:w/2+dx,y:h/2+p.y-c.y}}
const providers={osm:{url:(z,x,y)=>`https://tile.openstreetmap.org/${z}/${x}/${y}.png`,attr:'© OpenStreetMap-Mitwirkende'},topo:{url:(z,x,y)=>`https://a.tile.opentopomap.org/${z}/${x}/${y}.png`,attr:'Kartendaten © OpenStreetMap-Mitwirkende · Darstellung © OpenTopoMap'},sat:{url:(z,x,y)=>`https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/${z}/${y}/${x}`,attr:'Tiles © Esri World Imagery'},hybrid:{url:(z,x,y)=>`https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/${z}/${y}/${x}`,overlay:(z,x,y)=>`https://server.arcgisonline.com/ArcGIS/rest/services/Reference/World_Boundaries_and_Places/MapServer/tile/${z}/${y}/${x}`,attr:'Tiles © Esri World Imagery'}};
function setMapMode(mode){mapMode=mode;localStorage.setItem('jtrk-map',mode);document.querySelectorAll('[data-map]').forEach(b=>b.classList.toggle('active',b.dataset.map===mode));$('mapBadge').textContent=mode==='offline'?'Offline':mode.toUpperCase();$('attrib').textContent=providers[mode]?.attr||'Offline-Ansicht';drawMap()}
function tileImage(url,serial,draw){let img=tileCache.get(url);if(img){if(img.complete&&img.naturalWidth)draw(img);return}img=new Image();img.crossOrigin='anonymous';tileCache.set(url,img);img.onload=()=>{if(serial===renderSerial)draw(img)};img.onerror=()=>{};img.src=url}
function drawTiles(ctx,w,h,serial){if(mapMode==='offline'||!providers[mapMode])return;const z=view.z,c=world(view.lon,view.lat,z),left=c.x-w/2,top=c.y-h/2,minX=Math.floor(left/256),maxX=Math.floor((left+w)/256),minY=Math.floor(top/256),maxY=Math.floor((top+h)/256),n=2**z,p=providers[mapMode];for(let ty=minY;ty<=maxY;ty++){if(ty<0||ty>=n)continue;for(let tx=minX;tx<=maxX;tx++){const wx=((tx%n)+n)%n,dx=tx*256-left,dy=ty*256-top;tileImage(p.url(z,wx,ty),serial,img=>{ctx.drawImage(img,dx,dy,256,256);drawOverlay(ctx,w,h)});if(p.overlay)tileImage(p.overlay(z,wx,ty),serial,img=>{ctx.drawImage(img,dx,dy,256,256);drawOverlay(ctx,w,h)})}}}
function drawOverlay(ctx,w,h){if(mapMode==='offline'){ctx.strokeStyle='rgba(100,120,140,.25)';ctx.lineWidth=1;for(let i=1;i<5;i++){ctx.beginPath();ctx.moveTo(w*i/5,0);ctx.lineTo(w*i/5,h);ctx.stroke();ctx.beginPath();ctx.moveTo(0,h*i/5);ctx.lineTo(w,h*i/5);ctx.stroke()}}if(!track.length){ctx.fillStyle=getComputedStyle(document.body).color;ctx.textAlign='center';ctx.fillText('Keine Punkte in der aktuellen Auswahl',w/2,h/2);return}const pts=track.map(p=>toScreen(p.lat,p.lon,w,h));ctx.strokeStyle='#1463d6';ctx.lineWidth=3;ctx.lineJoin='round';ctx.beginPath();pts.forEach((p,i)=>i?ctx.lineTo(p.x,p.y):ctx.moveTo(p.x,p.y));ctx.stroke();pts.forEach((p,i)=>{ctx.beginPath();ctx.arc(p.x,p.y,i===0||i===pts.length-1?7:4,0,Math.PI*2);ctx.fillStyle=i===0?'#16834a':i===pts.length-1?'#c93434':'#1463d6';ctx.fill();ctx.strokeStyle='#fff';ctx.lineWidth=1.5;ctx.stroke()})}
function drawMap(){const {x,w,h}=canvas();renderSerial++;x.clearRect(0,0,w,h);if(mapMode==='offline'){x.fillStyle=getComputedStyle(document.body).backgroundColor;x.fillRect(0,0,w,h)}else{x.fillStyle='#d8dde3';x.fillRect(0,0,w,h);drawTiles(x,w,h,renderSerial)}drawOverlay(x,w,h)}
function fitTrack(){if(!track.length){drawMap();return}const minLat=Math.min(...track.map(p=>p.lat)),maxLat=Math.max(...track.map(p=>p.lat)),minLon=Math.min(...track.map(p=>p.lon)),maxLon=Math.max(...track.map(p=>p.lon));view.lat=(minLat+maxLat)/2;view.lon=(minLon+maxLon)/2;const {w,h}=canvas();for(let z=18;z>=2;z--){const a=world(minLon,maxLat,z),b=world(maxLon,minLat,z);if(Math.abs(b.x-a.x)<=w*.78&&Math.abs(b.y-a.y)<=h*.72){view.z=z;break}}drawMap()}
function zoomMap(delta){view.z=Math.max(2,Math.min(19,view.z+delta));drawMap()}
function showPoint(index){if(!track[index])return;const p=track[index],when=p.epoch?new Date(p.epoch*1000).toLocaleString('de-DE'):'Zeit unbekannt';$('pointInfo').textContent=(index+1)+'/'+track.length+' · '+(p.mgrs||'MGRS unbekannt')+' · '+when+' · '+p.source+(p.accuracy?' · ±'+Math.round(p.accuracy/1000)+' m':'')}
function applyFilter(){const d=$('trackDate').value,from=$('trackFrom').value||'00:00',to=$('trackTo').value||'23:59';if(!d){track=[...allTrack]}else{const start=new Date(d+'T'+from+':00').getTime()/1000,end=new Date(d+'T'+to+':59').getTime()/1000;track=allTrack.filter(p=>p.epoch>=start&&p.epoch<=end)}fitTrack();if(track.length)showPoint(track.length-1);$('mPoints').textContent=track.length;status('trackStatus',track.length+' Punkte in Auswahl.','ok')}
function quickFilter(seconds){if(!allTrack.length)return;const newest=Math.max(...allTrack.map(p=>p.epoch||0)),cut=newest-seconds;track=allTrack.filter(p=>(p.epoch||0)>=cut);$('trackDate').value='';fitTrack();if(track.length)showPoint(track.length-1);$('mPoints').textContent=track.length;status('trackStatus',track.length+' Punkte im Schnellfilter.','ok')}
function resetFilter(){$('trackDate').value='';$('trackFrom').value='00:00';$('trackTo').value='23:59';track=[...allTrack];fitTrack();if(track.length)showPoint(track.length-1);$('mPoints').textContent=track.length;status('trackStatus',track.length+' Punkte geladen.','ok')}
async function loadTrack(){try{status('trackStatus','Positionsverlauf wird geladen …');const r=await fetch('/track.geojson',{cache:'no-store'});if(!r.ok)throw Error(await r.text());const data=await r.json();allTrack=data.features.map(f=>({lon:f.geometry.coordinates[0],lat:f.geometry.coordinates[1],...f.properties}));track=[...allTrack];fitTrack();if(track.length)showPoint(track.length-1);else $('pointInfo').textContent='Noch keine gespeicherten Positionen.';$('mPoints').textContent=track.length;status('trackStatus',track.length+' Punkte geladen.','ok')}catch(e){status('trackStatus','Positionsverlauf nicht verfügbar: '+e.message,'err')}}
$('trackMap').addEventListener('click',e=>{if(!track.length)return;const r=e.currentTarget.getBoundingClientRect(),pts=track.map(p=>toScreen(p.lat,p.lon,r.width,r.height));let best=0,dist=Infinity;pts.forEach((p,i)=>{const d=(p.x-(e.clientX-r.left))**2+(p.y-(e.clientY-r.top))**2;if(d<dist){dist=d;best=i}});showPoint(best)});window.addEventListener('resize',drawMap);
async function clearTrack(){if(!confirm('Gesamten Positionsverlauf löschen?'))return;try{const r=await fetch('/track/clear',{method:'POST',headers:{'X-Jarnsen-Token':info.token}});if(!r.ok)throw Error(await r.text());allTrack=[];track=[];drawMap();$('pointInfo').textContent='Noch keine gespeicherten Positionen.';$('mPoints').textContent='0';status('trackStatus','Positionsverlauf gelöscht.','ok')}catch(e){status('trackStatus','Löschen fehlgeschlagen: '+e.message,'err')}}
async function analyse(){try{status('logStatus','Log wird geladen …');const r=await fetch('/log',{cache:'no-store'});if(!r.ok)throw Error(await r.text());const t=await r.text(),lines=t.split(/\r?\n/),count=x=>lines.filter(l=>l.includes(x)).length,last=[...lines].reverse().find(l=>l.includes(' | '))||'–',vals=[['Zeilen',lines.filter(Boolean).length],['Warnungen',count('WARN')+count('REJECT')],['Fehler/Resets',count('ERROR')+count('PANIC')+count('BROWNOUT')],['BLE',count('BLE_CONNECT')],['Position-TX',count('POSITION_TX')],['Loggröße',Math.round(new Blob([t]).size/1024)+' KB']];$('diagMetrics').innerHTML=vals.map(v=>'<div class="metric">'+v[0]+'<b>'+v[1]+'</b></div>').join('')+'<div class="metric" style="grid-column:1/-1">Letztes Ereignis<b style="font-size:13px">'+esc(last)+'</b></div>';$('diagMetrics').classList.remove('hide');$('raw').textContent=t;$('raw').classList.remove('hide');status('logStatus','Log vollständig geladen.','ok')}catch(e){status('logStatus','Logdownload fehlgeschlagen: '+e.message,'err')}}
async function clearLog(){if(!confirm('Diagnoselog vollständig löschen?'))return;try{const r=await fetch('/log/clear',{method:'POST',headers:{'X-Jarnsen-Token':info.token}});if(!r.ok)throw Error(await r.text());$('raw').textContent='';$('raw').classList.add('hide');$('diagMetrics').classList.add('hide');status('logStatus','Diagnoselog gelöscht.','ok')}catch(e){status('logStatus','Log löschen fehlgeschlagen: '+e.message,'err')}}
async function shutdownWifi(){if(!info)return;try{status('shutdownStatus','WLAN wird kontrolliert beendet …');const r=await fetch('/shutdown',{method:'POST',headers:{'X-Jarnsen-Token':info.token}});if(!r.ok)throw Error(await r.text());status('shutdownStatus','WLAN wird beendet. Danach wieder normalen Tracker-Service per Taste öffnen.','ok');$('barState').textContent='WLAN WIRD BEENDET'}catch(e){status('shutdownStatus','Beenden fehlgeschlagen: '+e.message,'err')}}
async function latest(){const r=await fetch(API+info.tag,{headers:{Accept:'application/vnd.github+json'},cache:'no-store'});if(!r.ok)throw Error('GitHub antwortet mit '+r.status);const release=await r.json();asset=release.assets.find(a=>a.name===info.asset);if(!asset||!asset.digest?.startsWith('sha256:'))throw Error('Passende geprüfte Firmware fehlt im Release');return asset}function resetProgress(){const p=$('progress');p.value=0;p.classList.add('hide')}
async function githubUpdate(){try{status('fwStatus','GitHub-Release wird geprüft …');const a=await latest();$('downloadLink').href=a.browser_download_url;status('fwStatus','Firmware wird direkt aus GitHub geladen …');const r=await fetch(a.browser_download_url,{cache:'no-store'});if(!r.ok)throw Error('Download '+r.status);await upload(await r.blob(),a)}catch(e){resetProgress();$('fallback').classList.remove('hide');status('fwStatus','Direkte Übergabe nicht möglich: '+e.message,'err')}}async function uploadSelected(){try{const f=$('file').files[0];if(!f)throw Error('Bitte zuerst die .bin-Datei auswählen');await upload(f,asset||await latest())}catch(e){resetProgress();status('fwStatus',e.message,'err')}}async function upload(blob,a){if(blob.size!==a.size)throw Error('Dateigröße passt nicht zum GitHub-Release');const expected=a.digest.slice(7).toLowerCase();if(!/^[0-9a-f]{64}$/.test(expected))throw Error('GitHub liefert keine gültige SHA-256-Prüfsumme');if(!confirm('Firmware für '+info.title+' installieren? Der Node startet danach neu.'))return;const p=$('progress');p.classList.remove('hide');p.value=0;status('fwStatus','Firmware wird übertragen und im Node per SHA-256 geprüft …');await new Promise((resolve,reject)=>{const x=new XMLHttpRequest();x.open('POST','/update');x.setRequestHeader('Content-Type','application/octet-stream');x.setRequestHeader('X-Jarnsen-Token',info.token);x.setRequestHeader('X-Jarnsen-Device',info.device);x.setRequestHeader('X-Jarnsen-Sha256',expected);x.upload.onprogress=e=>{if(e.lengthComputable)p.value=Math.round(e.loaded*100/e.total)};x.onload=()=>x.status===200?resolve():reject(Error(x.responseText||'Update fehlgeschlagen'));x.onerror=()=>reject(Error('WLAN-Verbindung unterbrochen'));x.send(blob)});p.value=100;status('fwStatus','Update geprüft. Node startet neu.','ok')}
boot();
</script></body></html>)HTML";

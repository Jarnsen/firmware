#pragma once

#include <Arduino.h>

static const char JARNSEN_V3_PORTAL_PAGE[] PROGMEM = R"HTML(<!doctype html>
<html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#0b1220">
<title>Jarnsen V3 Service</title><style>
:root{color-scheme:light dark;--bg:#eef2f7;--card:#fff;--fg:#17212b;--muted:#677586;--line:#d8e0e8;--accent:#1463d6;--ok:#16834a;--warn:#c26a00;--bad:#c93434;--chip:#eaf1f8}
@media(prefers-color-scheme:dark){:root{--bg:#0b1220;--card:#121d2f;--fg:#eaf2ff;--muted:#91a6c2;--line:#263a55;--accent:#38bdf8;--ok:#3ddc97;--warn:#ffb454;--bad:#ff6b77;--chip:#182741}}
*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--bg);color:var(--fg);font:15px system-ui,-apple-system,"Segoe UI",sans-serif}.wrap{max-width:820px;margin:auto;padding:12px 12px 38px}.servicebar{position:sticky;top:0;z-index:20;margin:-12px -12px 12px;padding:10px 12px;background:color-mix(in srgb,var(--card) 94%,transparent);border-bottom:1px solid var(--line);backdrop-filter:blur(12px);display:flex;align-items:center;gap:8px}.servicebar .grow{min-width:0;flex:1}.servicebar b,.servicebar small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.servicebar small{color:var(--muted)}h1{font-size:24px;margin:3px 0}.sub,.muted{color:var(--muted)}.nav{display:flex;gap:7px;overflow:auto;padding:3px 0 6px}.nav a,.chip{white-space:nowrap;background:var(--chip);color:var(--fg);padding:8px 10px;border-radius:999px;text-decoration:none;border:1px solid var(--line)}.card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:14px;margin:12px 0;box-shadow:0 2px 12px #0001}.card h2{font-size:18px;margin:0 0 10px}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.metric{border:1px solid var(--line);border-radius:12px;padding:10px;min-width:0;color:var(--muted)}.metric b{display:block;color:var(--fg);font-size:18px;margin-top:3px;overflow:hidden;text-overflow:ellipsis}.actions,.mapmodes,.quick{display:flex;flex-wrap:wrap;gap:7px}button,.button,select,input{font:inherit}button,.button{appearance:none;border:0;border-radius:10px;background:var(--accent);color:#fff;padding:10px 12px;font-weight:650;text-decoration:none;text-align:center;cursor:pointer}button.secondary,.button.secondary{background:transparent;color:var(--accent);border:1px solid var(--accent)}button.danger{background:var(--bad)}button.active{outline:2px solid var(--fg);outline-offset:1px}button:disabled{opacity:.45}.status{min-height:21px;margin-top:8px;color:var(--muted)}.ok{color:var(--ok)}.warn{color:var(--warn)}.err{color:var(--bad)}.filters{display:grid;grid-template-columns:1.4fr 1fr 1fr;gap:7px;margin:9px 0}.filters label{font-size:12px;color:var(--muted)}.filters input{display:block;width:100%;margin-top:3px;padding:9px;border:1px solid var(--line);border-radius:9px;background:var(--bg);color:var(--fg)}.mapbox{position:relative;height:390px;border:1px solid var(--line);border-radius:13px;overflow:hidden;background:#d8dde3;margin-top:8px}.mapbox canvas{width:100%;height:100%;display:block;touch-action:none}.mapbadge{position:absolute;z-index:4;left:8px;top:8px;background:color-mix(in srgb,var(--card) 90%,transparent);border:1px solid var(--line);border-radius:8px;padding:5px 7px;font-size:12px;color:var(--muted);pointer-events:none}.attrib{font-size:11px;color:var(--muted);min-height:17px;margin-top:4px}.pointinfo{min-height:42px;padding:8px 1px;color:var(--muted)}progress{width:100%;height:12px;margin-top:10px}pre{white-space:pre-wrap;word-break:break-word;max-height:38vh;overflow:auto;background:var(--bg);border-radius:10px;padding:10px;font:12px ui-monospace,monospace}.hide{display:none}.switch{display:inline-flex;align-items:center;gap:7px;padding:8px 10px;border:1px solid var(--line);border-radius:10px}.switch input{width:18px;height:18px}.dangerzone{border-color:color-mix(in srgb,var(--bad) 35%,var(--line))}@media(max-width:620px){.grid{grid-template-columns:repeat(2,minmax(0,1fr))}.filters{grid-template-columns:1fr 1fr}.filters label:first-child{grid-column:1/-1}.mapbox{height:330px}.actions>*{flex:1 1 145px}}@media(max-width:390px){.grid{grid-template-columns:1fr 1fr}.metric b{font-size:16px}}
</style></head><body><main class="wrap">
<div class="servicebar"><div class="grow"><b id="barName">Jarnsen V3</b><small id="barState">WLAN SERVICE · 192.168.4.1</small></div><button class="danger" onclick="shutdownWifi()">WLAN beenden</button></div>
<h1>Jarnsen Mobile Service</h1><div class="sub" id="device">Gerät wird gelesen …</div><nav class="nav"><a href="#overview">Übersicht</a><a href="#map">Karte</a><a href="#diag">Diagnose</a><a href="#mesh">Mesh / Power</a><a href="#firmware">Firmware</a></nav>
<section class="card" id="overview"><h2>Übersicht</h2><div class="grid"><div class="metric">Node<b id="mNode">–</b></div><div class="metric">Rolle<b id="mRole">–</b></div><div class="metric">WLAN<b id="mSsid">–</b></div><div class="metric">Positionen<b id="mPoints">0</b></div><div class="metric">Firmware<b id="mFw">Log laden</b></div><div class="metric">Akku<b id="mBat">–</b></div></div><div id="overviewStatus" class="status">Lokale Servicedaten bereit.</div></section>
<section class="card" id="map"><h2>Positionskarte</h2><div class="mapmodes"><button data-map="osm" onclick="setMapMode('osm')">OSM</button><button data-map="topo" onclick="setMapMode('topo')">Topo</button><button data-map="sat" onclick="setMapMode('sat')">Satellit</button><button data-map="hybrid" onclick="setMapMode('hybrid')">Hybrid</button><button data-map="offline" onclick="setMapMode('offline')">Offline</button><label class="switch"><input id="mgrsGrid" type="checkbox" onchange="toggleMgrs()">MGRS-Gitter</label></div>
<div class="filters"><label>Datum<input id="trackDate" type="date" onchange="applyCustomFilter()"></label><label>Von<input id="trackFrom" type="time" value="00:00" onchange="applyCustomFilter()"></label><label>Bis<input id="trackTo" type="time" value="23:59" onchange="applyCustomFilter()"></label></div><div class="quick"><button class="secondary" onclick="quickFilter(3600)">1 h</button><button class="secondary" onclick="quickFilter(21600)">6 h</button><button class="secondary" onclick="quickFilter(86400)">24 h</button><button class="secondary" onclick="quickFilter(604800)">7 Tage</button><button class="secondary" onclick="resetFilter()">Alle</button></div>
<div class="mapbox"><canvas id="trackMap"></canvas><div class="mapbadge" id="mapBadge">Offline</div></div><div class="attrib" id="attrib"></div><div class="pointinfo" id="pointInfo">Punkte werden geladen …</div><div class="actions"><button onclick="loadTrack()">Neu laden</button><button class="secondary" onclick="fitTrack()">Auf Auswahl zoomen</button><button class="secondary" onclick="zoomMap(1)">+</button><button class="secondary" onclick="zoomMap(-1)">−</button><a class="button secondary" href="/track.geojson" download="Jarnsen-Positionsverlauf.geojson">GeoJSON</a><button class="danger" onclick="clearTrack()">Verlauf löschen</button></div><div id="trackStatus" class="status"></div></section>
<section class="card" id="diag"><h2>Diagnose</h2><div class="actions"><button onclick="analyse()">Log laden &amp; auswerten</button><a class="button secondary" href="/log" download>Log speichern</a><button class="danger" onclick="clearLog()">Log löschen</button></div><div id="logStatus" class="status"></div><div id="diagMetrics" class="grid hide"></div><pre id="raw" class="hide"></pre></section>
<section class="card" id="mesh"><h2>Mesh / Power</h2><div class="grid"><div class="metric">Spannung<b id="pVolt">–</b></div><div class="metric">Akku<b id="pPct">–</b></div><div class="metric">Restlaufzeit<b id="pEst">–</b></div><div class="metric">Listen<b id="pListen">–</b></div><div class="metric">Service<b id="pService">–</b></div><div class="metric">BLE<b id="pBle">–</b></div><div class="metric">Display<b id="pDisp">–</b></div><div class="metric">TX<b id="pTx">–</b></div><div class="metric">Resets / Panic<b id="pReset">–</b></div></div><div class="status">Werte werden beim Laden des Diagnoselogs aktualisiert.</div></section>
<section class="card" id="firmware"><h2>Firmwareupdate über WLAN</h2><p class="muted">Die passende aktuelle Firmware wird aus dem Jarnsen-GitHub-Release gewählt und vor Installation per SHA-256 geprüft.</p><div class="actions"><button id="githubBtn" onclick="githubUpdate()">Aktuelle GitHub-Firmware laden</button></div><div id="fallback" class="hide"><p class="warn">Der Captive-Browser konnte die GitHub-Datei nicht direkt weiterreichen. Datei laden und anschließend hier auswählen.</p><a id="downloadLink" class="button secondary" target="_blank">Firmware aus GitHub laden</a><input id="file" type="file" accept=".bin,application/octet-stream"><button onclick="uploadSelected()">Ausgewählte Firmware installieren</button></div><progress id="progress" class="hide" max="100" value="0"></progress><div id="fwStatus" class="status"></div></section>
<section class="card dangerzone"><h2>Service beenden</h2><p class="muted">Beendet den WLAN-AP kontrolliert. Der V3 stellt danach den normalen Repeaterbetrieb wieder her; Bluetooth ist beim nächsten GPIO0-Service wieder verfügbar.</p><button class="danger" onclick="shutdownWifi()">WLAN jetzt beenden</button><div id="shutdownStatus" class="status"></div></section>
</main><script>
const API='https://api.github.com/repos/Jarnsen/firmware/releases/tags/';const $=id=>document.getElementById(id);let info=null,asset=null,allTrack=[],track=[],view={lat:49,lon:8,z:8},mapMode=localStorage.getItem('jv3-map')||'offline',mgrsOn=localStorage.getItem('jv3-mgrs')==='1',tileCache=new Map(),tileErrors=0,renderSerial=0;
function status(id,text,kind=''){const e=$(id);if(!e)return;e.textContent=text;e.className='status '+kind}function esc(s){return String(s??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}function fmtSec(v){v=Number(v);if(!Number.isFinite(v))return'–';const d=Math.floor(v/86400),h=Math.floor(v%86400/3600),m=Math.floor(v%3600/60);return(d?d+'d ':'')+(h?h+'h ':'')+m+'m'}
async function boot(){try{const r=await fetch('/status',{cache:'no-store'});info=await r.json();$('device').textContent=(info.long_name||info.title)+' · '+info.device+' · '+info.node_id;$('barName').textContent=info.long_name||'Jarnsen V3';$('mNode').textContent=info.short_name||info.node_id;$('mRole').textContent=info.role||'–';$('mSsid').textContent=info.ssid;$('mPoints').textContent=info.track_count;$('barState').textContent='WLAN SERVICE · 192.168.4.1';}catch(e){status('overviewStatus','Status nicht lesbar: '+e.message,'err')}setMapMode(mapMode);$('mgrsGrid').checked=mgrsOn;await loadTrack()}
function canvas(){const c=$('trackMap'),r=c.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);const w=Math.max(1,r.width),h=Math.max(1,r.height);if(c.width!==Math.round(w*d)||c.height!==Math.round(h*d)){c.width=Math.round(w*d);c.height=Math.round(h*d)}const x=c.getContext('2d');x.setTransform(d,0,0,d,0,0);return{c,x,w,h}}
function clampLat(v){return Math.max(-85.05112878,Math.min(85.05112878,v))}function world(lon,lat,z){const s=256*Math.pow(2,z),la=clampLat(lat)*Math.PI/180;return{x:(lon+180)/360*s,y:(1-Math.log(Math.tan(la)+1/Math.cos(la))/Math.PI)/2*s}}function unworld(x,y,z){const s=256*Math.pow(2,z),lon=x/s*360-180,n=Math.PI-2*Math.PI*y/s;return{lon,lat:180/Math.PI*Math.atan(Math.sinh(n))}}function toScreen(lat,lon,w,h){const p=world(lon,lat,view.z),c=world(view.lon,view.lat,view.z),s=256*Math.pow(2,view.z);let dx=p.x-c.x;if(dx>s/2)dx-=s;if(dx<-s/2)dx+=s;return{x:w/2+dx,y:h/2+p.y-c.y}}function fromScreen(x,y,w,h){const c=world(view.lon,view.lat,view.z);return unworld(c.x+x-w/2,c.y+y-h/2,view.z)}
const providers={osm:{url:(z,x,y)=>`https://tile.openstreetmap.org/${z}/${x}/${y}.png`,attr:'© OpenStreetMap-Mitwirkende'},topo:{url:(z,x,y)=>`https://a.tile.opentopomap.org/${z}/${x}/${y}.png`,attr:'Kartendaten © OpenStreetMap-Mitwirkende, SRTM · Darstellung © OpenTopoMap (CC-BY-SA)'},sat:{url:(z,x,y)=>`https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/${z}/${y}/${x}`,attr:'Tiles © Esri · World Imagery'},hybrid:{url:(z,x,y)=>`https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/${z}/${y}/${x}`,overlay:(z,x,y)=>`https://server.arcgisonline.com/ArcGIS/rest/services/Reference/World_Boundaries_and_Places/MapServer/tile/${z}/${y}/${x}`,attr:'Tiles © Esri · World Imagery + Boundaries/Places'}};
function setMapMode(m){mapMode=providers[m]?m:'offline';localStorage.setItem('jv3-map',mapMode);tileErrors=0;document.querySelectorAll('[data-map]').forEach(b=>b.classList.toggle('active',b.dataset.map===mapMode));$('mapBadge').textContent=mapMode==='offline'?'Offline':mapMode==='sat'?'Satellit':mapMode[0].toUpperCase()+mapMode.slice(1);$('attrib').textContent=providers[mapMode]?.attr||'Offline-Ansicht · keine Kartendaten werden geladen';renderMap()}
function tileImage(url,serial){let e=tileCache.get(url);if(e){if(e.ok)return e.img;return null}const img=new Image();e={img,ok:false};tileCache.set(url,e);img.onload=()=>{e.ok=true;if(serial===renderSerial)renderMap()};img.onerror=()=>{e.bad=true;tileErrors++;if(tileErrors>=4&&mapMode!=='offline'){mapMode='offline';localStorage.setItem('jv3-map','offline');status('trackStatus','Online-Karte nicht erreichbar – Offline-Ansicht aktiv.','warn');setMapMode('offline')}};img.src=url;return null}
function drawTiles(x,w,h,serial){x.fillStyle='#dbe1e7';x.fillRect(0,0,w,h);if(mapMode==='offline')return;const p=providers[mapMode],c=world(view.lon,view.lat,view.z),n=Math.pow(2,view.z),minX=Math.floor((c.x-w/2)/256),maxX=Math.floor((c.x+w/2)/256),minY=Math.max(0,Math.floor((c.y-h/2)/256)),maxY=Math.min(n-1,Math.floor((c.y+h/2)/256));for(let ty=minY;ty<=maxY;ty++)for(let tx=minX;tx<=maxX;tx++){const xx=(tx%n+n)%n,dx=tx*256-(c.x-w/2),dy=ty*256-(c.y-h/2),u=p.url(view.z,xx,ty),im=tileImage(u,serial);if(im)x.drawImage(im,dx,dy,256,256);if(p.overlay){const o=tileImage(p.overlay(view.z,xx,ty),serial);if(o)x.drawImage(o,dx,dy,256,256)}}}
function zoneFor(lat,lon){let z=Math.max(1,Math.min(60,Math.floor((lon+180)/6)+1));if(lat>=56&&lat<64&&lon>=3&&lon<12)z=32;if(lat>=72&&lat<84){if(lon<9&&lon>=0)z=31;else if(lon<21)z=33;else if(lon<33)z=35;else if(lon<42)z=37}return z}function latBand(lat){const b='CDEFGHJKLMNPQRSTUVWX';if(lat<-80||lat>84)return'?';return b[Math.max(0,Math.min(19,Math.floor((lat+80)/8)))]}
function utmForward(lat,lon,forceZone){const a=6378137,e2=.00669438,k=.9996,rad=Math.PI/180,z=forceZone||zoneFor(lat,lon),lo=((z-1)*6-180+3)*rad,la=lat*rad,lr=lon*rad,ep=e2/(1-e2),s=Math.sin(la),c=Math.cos(la),t=Math.tan(la),N=a/Math.sqrt(1-e2*s*s),T=t*t,C=ep*c*c,A=c*(lr-lo),M=a*((1-e2/4-3*e2*e2/64-5*e2**3/256)*la-(3*e2/8+3*e2*e2/32+45*e2**3/1024)*Math.sin(2*la)+(15*e2*e2/256+45*e2**3/1024)*Math.sin(4*la)-(35*e2**3/3072)*Math.sin(6*la));let E=k*N*(A+(1-T+C)*A**3/6+(5-18*T+T*T+72*C-58*ep)*A**5/120)+500000,Nt=k*(M+N*t*(A*A/2+(5-T+9*C+4*C*C)*A**4/24+(61-58*T+T*T+600*C-330*ep)*A**6/720));if(lat<0)Nt+=1e7;return{zone:z,north:lat>=0,e:E,n:Nt,band:latBand(lat)}}
function utmInverse(zone,north,E,Nt){const a=6378137,e2=.00669438,k=.9996,ep=e2/(1-e2),x=E-500000;let y=Nt;if(!north)y-=1e7;const M=y/k,mu=M/(a*(1-e2/4-3*e2*e2/64-5*e2**3/256)),e1=(1-Math.sqrt(1-e2))/(1+Math.sqrt(1-e2)),p1=mu+(3*e1/2-27*e1**3/32)*Math.sin(2*mu)+(21*e1*e1/16-55*e1**4/32)*Math.sin(4*mu)+(151*e1**3/96)*Math.sin(6*mu)+(1097*e1**4/512)*Math.sin(8*mu),s=Math.sin(p1),c=Math.cos(p1),t=Math.tan(p1),N1=a/Math.sqrt(1-e2*s*s),R1=a*(1-e2)/Math.pow(1-e2*s*s,1.5),T1=t*t,C1=ep*c*c,D=x/(N1*k),lat=p1-(N1*t/R1)*(D*D/2-(5+3*T1+10*C1-4*C1*C1-9*ep)*D**4/24+(61+90*T1+298*C1+45*T1*T1-252*ep-3*C1*C1)*D**6/720),lon=((zone-1)*6-180+3)*Math.PI/180+(D-(1+2*T1+C1)*D**3/6+(5-2*C1+28*T1-3*C1*C1+8*ep+24*T1*T1)*D**5/120)/c;return{lat:lat*180/Math.PI,lon:lon*180/Math.PI}}
function mgrsFromUtm(u,precision=5){const sets=['ABCDEFGH','JKLMNPQR','STUVWXYZ'],rows='ABCDEFGHJKLMNPQRSTUV',e100=Math.floor(u.e/100000);if(e100<1||e100>8)return'---';const el=sets[(u.zone-1)%3][e100-1],nl=rows[(Math.floor(u.n/100000)%20+(u.zone%2===0?5:0))%20],ep=String((Math.floor(u.e)%100000+100000)%100000).padStart(5,'0').slice(0,precision),np=String((Math.floor(u.n)%100000+100000)%100000).padStart(5,'0').slice(0,precision);return String(u.zone).padStart(2,'0')+u.band+' '+el+nl+(precision?' '+ep+' '+np:'')}function mgrs10(lat,lon){return mgrsFromUtm(utmForward(lat,lon),5)}
function toggleMgrs(){mgrsOn=$('mgrsGrid').checked;localStorage.setItem('jv3-mgrs',mgrsOn?'1':'0');renderMap()}function gridStep(){return view.z<=8?100000:view.z<=11?10000:view.z<=14?1000:view.z<=16?100:10}function drawMgrsGrid(x,w,h){if(!mgrsOn)return;const center=utmForward(view.lat,view.lon),zone=center.zone,north=center.north,corners=[[0,0],[w,0],[0,h],[w,h]].map(q=>fromScreen(q[0],q[1],w,h)).map(p=>utmForward(p.lat,p.lon,zone));let minE=Math.min(...corners.map(p=>p.e)),maxE=Math.max(...corners.map(p=>p.e)),minN=Math.min(...corners.map(p=>p.n)),maxN=Math.max(...corners.map(p=>p.n)),step=gridStep();while((maxE-minE)/step>16||(maxN-minN)/step>16)step*=10;const digits=Math.max(0,Math.min(5,5-Math.round(Math.log10(step))));x.save();x.strokeStyle='rgba(255,190,0,.72)';x.fillStyle='rgba(15,20,25,.82)';x.lineWidth=1;x.font='10px system-ui';for(let e=Math.ceil(minE/step)*step;e<=maxE;e+=step){x.beginPath();let first=true;for(let i=0;i<=10;i++){const n=minN+(maxN-minN)*i/10,p=utmInverse(zone,north,e,n),s=toScreen(p.lat,p.lon,w,h);if(first){x.moveTo(s.x,s.y);first=false}else x.lineTo(s.x,s.y)}x.stroke();const mid=utmInverse(zone,north,e,(minN+maxN)/2),s=toScreen(mid.lat,mid.lon,w,h),label=mgrsFromUtm({...utmForward(mid.lat,mid.lon,zone),e},digits);x.fillText(label,Math.max(3,Math.min(w-105,s.x+2)),12)}for(let n=Math.ceil(minN/step)*step;n<=maxN;n+=step){x.beginPath();let first=true;for(let i=0;i<=10;i++){const e=minE+(maxE-minE)*i/10,p=utmInverse(zone,north,e,n),s=toScreen(p.lat,p.lon,w,h);if(first){x.moveTo(s.x,s.y);first=false}else x.lineTo(s.x,s.y)}x.stroke()}x.restore()}
function drawTrack(x,w,h){if(!track.length)return;const pts=track.map(p=>toScreen(p.lat,p.lon,w,h));x.strokeStyle='#1463d6';x.lineWidth=3;x.lineJoin='round';x.beginPath();pts.forEach((p,i)=>i?x.lineTo(p.x,p.y):x.moveTo(p.x,p.y));x.stroke();pts.forEach((p,i)=>{x.beginPath();x.arc(p.x,p.y,i===0||i===pts.length-1?6:3.5,0,Math.PI*2);x.fillStyle=i===0?'#16834a':i===pts.length-1?'#c93434':'#1463d6';x.fill();x.strokeStyle='#fff';x.lineWidth=1.2;x.stroke()})}
function renderMap(){const {x,w,h}=canvas(),serial=++renderSerial;drawTiles(x,w,h,serial);drawMgrsGrid(x,w,h);drawTrack(x,w,h)}function fitTrack(){if(!track.length){renderMap();return}let minLat=Math.min(...track.map(p=>p.lat)),maxLat=Math.max(...track.map(p=>p.lat)),minLon=Math.min(...track.map(p=>p.lon)),maxLon=Math.max(...track.map(p=>p.lon));view.lat=(minLat+maxLat)/2;view.lon=(minLon+maxLon)/2;let z=17;const {w,h}=canvas();for(;z>3;z--){const a=world(minLon,maxLat,z),b=world(maxLon,minLat,z);if(Math.abs(b.x-a.x)<=w*.78&&Math.abs(b.y-a.y)<=h*.72)break}view.z=Math.max(3,Math.min(19,z));renderMap()}function zoomMap(d){view.z=Math.max(3,Math.min(19,view.z+d));renderMap()}
function distance(a,b){const r=6371000,rad=Math.PI/180,p1=a.lat*rad,p2=b.lat*rad,dp=(b.lat-a.lat)*rad,dl=(b.lon-a.lon)*rad,q=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*r*Math.atan2(Math.sqrt(q),Math.sqrt(1-q))}function updateTrackSummary(){let d=0;for(let i=1;i<track.length;i++)d+=distance(track[i-1],track[i]);if(!track.length){$('pointInfo').textContent='Keine Punkte im gewählten Zeitraum.';return}const a=new Date(track[0].epoch*1000),b=new Date(track.at(-1).epoch*1000);$('pointInfo').textContent=track.length+' Punkte · '+a.toLocaleString('de-DE')+' – '+b.toLocaleString('de-DE')+' · '+(d>=1000?(d/1000).toFixed(1)+' km':Math.round(d)+' m')}
function applyRange(start,end){track=allTrack.filter(p=>(!start||p.epoch*1000>=start)&&(!end||p.epoch*1000<=end));$('mPoints').textContent=track.length;updateTrackSummary();fitTrack()}function quickFilter(sec){$('trackDate').value='';const ref=(allTrack.length?allTrack.at(-1).epoch*1000:Date.now());applyRange(ref-sec*1000,ref)}function resetFilter(){$('trackDate').value='';$('trackFrom').value='00:00';$('trackTo').value='23:59';applyRange(0,0)}function applyCustomFilter(){const d=$('trackDate').value;if(!d){applyRange(0,0);return}const from=$('trackFrom').value||'00:00',to=$('trackTo').value||'23:59',a=new Date(d+'T'+from+':00'),b=new Date(d+'T'+to+':59');applyRange(a.getTime(),b.getTime())}
async function loadTrack(){try{status('trackStatus','Positionsverlauf wird geladen …');const r=await fetch('/track.geojson',{cache:'no-store'});if(!r.ok)throw Error(await r.text());const data=await r.json();allTrack=data.features.map(f=>({lon:f.geometry.coordinates[0],lat:f.geometry.coordinates[1],...f.properties})).sort((a,b)=>a.epoch-b.epoch);track=[...allTrack];$('mPoints').textContent=track.length;if(allTrack.length&&!$('trackDate').value){const d=new Date(allTrack.at(-1).epoch*1000);$('trackDate').value=[d.getFullYear(),String(d.getMonth()+1).padStart(2,'0'),String(d.getDate()).padStart(2,'0')].join('-')}updateTrackSummary();fitTrack();status('trackStatus',allTrack.length+' gespeicherte Punkte geladen.','ok')}catch(e){status('trackStatus','Positionsverlauf nicht verfügbar: '+e.message,'err')}}
$('trackMap').addEventListener('click',e=>{if(!track.length)return;const r=e.currentTarget.getBoundingClientRect();let best=track[0],bd=Infinity;for(const p of track){const s=toScreen(p.lat,p.lon,r.width,r.height),d=(s.x-(e.clientX-r.left))**2+(s.y-(e.clientY-r.top))**2;if(d<bd){bd=d;best=p}}$('pointInfo').textContent=mgrs10(best.lat,best.lon)+' · '+new Date(best.epoch*1000).toLocaleString('de-DE')+' · '+best.source+(best.accuracy?' · ±'+Math.round(best.accuracy/1000)+' m':'')});window.addEventListener('resize',renderMap);
function header(text,key){const m=text.match(new RegExp('^# '+key+'=([^\\r\\n]+)','m'));return m?m[1].trim():''}function token(line,key){const m=line.match(new RegExp('(?:^|\\s)'+key+'=([^\\s]+)'));return m?m[1]:''}
async function analyse(){try{status('logStatus','Log wird geladen …');const r=await fetch('/log',{cache:'no-store'});if(!r.ok)throw Error(await r.text());const t=await r.text(),lines=t.split(/\r?\n/),count=s=>lines.filter(l=>l.includes(s)).length,lastBat=[...lines].reverse().find(l=>l.startsWith('LIVE | BATTERY')||l.includes('| BATTERY'))||'',vals=[['Zeilen',lines.filter(Boolean).length],['Warnungen',count('WARN')+count('REJECT')],['PANIC/Fehler',count('PANIC')+count('ERROR')+count('BROWNOUT')],['BLE Connect',count('BLE_CONNECT')],['Position TX',count('PHONE_POS_LIVE')+count('POSITION_TX')],['Loggröße',Math.round(new Blob([t]).size/1024)+' KB']];$('diagMetrics').innerHTML=vals.map(v=>'<div class="metric">'+v[0]+'<b>'+v[1]+'</b></div>').join('');$('diagMetrics').classList.remove('hide');$('raw').textContent=t;$('raw').classList.remove('hide');const fw=header(t,'firmware'),build=header(t,'build');$('mFw').textContent=fw?(fw.replace(/^2\.8\.0\./,'')+(build?' · '+build.slice(0,8):'')):'–';const mv=(lastBat.match(/(?:^|\s)(\d+)mV(?:\s|$)/)||[])[1],pct=(lastBat.match(/(?:^|\s)(\d+)%/)||[])[1];$('mBat').textContent=pct?pct+'%':'–';$('pVolt').textContent=mv?mv+' mV':'–';$('pPct').textContent=pct?pct+'%':'–';$('pEst').textContent=token(lastBat,'est')||'–';$('pListen').textContent=fmtSec(token(lastBat,'listen'));$('pService').textContent=fmtSec(token(lastBat,'service'));$('pBle').textContent=fmtSec(token(lastBat,'ble'));$('pDisp').textContent=fmtSec(token(lastBat,'disp'));$('pTx').textContent=token(lastBat,'tx')||'–';$('pReset').textContent=String(count('reset=PANIC')+count('PANIC'));status('logStatus','Log vollständig geladen und ausgewertet.','ok');status('overviewStatus','Diagnosewerte aktualisiert.','ok')}catch(e){status('logStatus','Logdownload fehlgeschlagen: '+e.message,'err')}}
async function clearTrack(){if(!confirm('Gesamten Positionsverlauf löschen?'))return;try{const r=await fetch('/track/clear',{method:'POST',headers:{'X-Jarnsen-Token':info.token}});if(!r.ok)throw Error(await r.text());allTrack=[];track=[];updateTrackSummary();renderMap();$('mPoints').textContent='0';status('trackStatus','Positionsverlauf gelöscht.','ok')}catch(e){status('trackStatus','Löschen fehlgeschlagen: '+e.message,'err')}}async function clearLog(){if(!confirm('Diagnoselog wirklich löschen?'))return;try{const r=await fetch('/log/clear',{method:'POST',headers:{'X-Jarnsen-Token':info.token}});if(!r.ok)throw Error(await r.text());$('raw').classList.add('hide');$('diagMetrics').classList.add('hide');status('logStatus','Diagnoselog gelöscht.','ok')}catch(e){status('logStatus','Log löschen fehlgeschlagen: '+e.message,'err')}}
async function shutdownWifi(){if(!info||!confirm('WLAN-Service jetzt beenden?'))return;try{status('shutdownStatus','WLAN wird beendet …');const r=await fetch('/shutdown',{method:'POST',headers:{'X-Jarnsen-Token':info.token}});if(!r.ok)throw Error(await r.text());$('barState').textContent='WLAN wird beendet · Repeater wird wiederhergestellt';status('shutdownStatus','AP wird geschlossen. Beim nächsten GPIO0-Service steht Bluetooth wieder bereit.','ok')}catch(e){status('shutdownStatus','Beenden fehlgeschlagen: '+e.message,'err')}}
async function latest(){const r=await fetch(API+info.tag,{headers:{Accept:'application/vnd.github+json'}});if(!r.ok)throw Error('GitHub Release '+r.status);const j=await r.json(),man=j.assets.find(a=>a.name===info.asset.replace('.update.bin','.ota.json')),bin=j.assets.find(a=>a.name===info.asset);if(!bin)throw Error('Firmware-Datei nicht gefunden');asset=bin;let sha='';if(man){const mr=await fetch(man.browser_download_url);if(mr.ok){const mj=await mr.json();sha=mj.sha256||''}}return{bin,sha}}
function hex(buf){return [...new Uint8Array(buf)].map(x=>x.toString(16).padStart(2,'0')).join('')}async function sha256(blob){return hex(await crypto.subtle.digest('SHA-256',await blob.arrayBuffer()))}async function postFirmware(blob,expected){$('progress').classList.remove('hide');$('progress').value=15;const got=await sha256(blob);if(expected&&got.toLowerCase()!==expected.toLowerCase())throw Error('SHA-256 der Firmware stimmt nicht');$('progress').value=35;const r=await fetch('/update',{method:'POST',headers:{'Content-Type':'application/octet-stream','X-Jarnsen-Device':info.device,'X-Jarnsen-Sha256':got,'X-Jarnsen-Token':info.token},body:blob});if(!r.ok)throw Error(await r.text());$('progress').value=100;status('fwStatus','Firmware installiert. Node startet neu.','ok')}
async function githubUpdate(){try{status('fwStatus','GitHub-Release wird geprüft …');const a=await latest();$('downloadLink').href=a.bin.browser_download_url;const r=await fetch(a.bin.browser_download_url);if(!r.ok)throw Error('Direkter Download nicht möglich');const blob=await r.blob();await postFirmware(blob,a.sha)}catch(e){$('fallback').classList.remove('hide');status('fwStatus','Direkte Installation nicht möglich: '+e.message,'warn')}}async function uploadSelected(){try{const f=$('file').files[0];if(!f)throw Error('Keine Firmware gewählt');const a=asset?{sha:''}:await latest();await postFirmware(f,a.sha||'')}catch(e){status('fwStatus','Update fehlgeschlagen: '+e.message,'err')}}
$('file').addEventListener('change',()=>status('fwStatus',$('file').files[0]?.name||''));boot();
</script></body></html>)HTML";
